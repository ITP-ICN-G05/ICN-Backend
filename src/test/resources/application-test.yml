GOOGLE_MAPS_API_KEY: AIzaSyBsJb-fpJij7dFzZC9QbO4DsGXlw1c2-n0
EMAIL: lys.2026485907@gmail.com
EMAIL_PASS: xeqbzbnzvvutvryo

pebble:
  # default ".peb" to "":
  suffix:
  cache: false
spring:
  application:
    name: ICN_Back
  data:
    mongodb:
      uri: mongodb://localhost:27017/ICN_Navigator_Test
  main:
    allow-bean-definition-overriding: true
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

  ## email settings
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL}
    password: ${EMAIL_PASS}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: true

# 日志配置
logging:
  level:
    com.gof.ICNBack.Controller: DEBUG
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.springframework.data.mongodb.core: DEBUG
    com.gof.ICNBack.Repositories: DEBUG


test:
  security:
    enabled: false

app:
  google-map-geocode:
    enable: 0
    api-key: ${GOOGLE_MAPS_API_KEY}
    geocoding-url: https://maps.googleapis.com/maps/api/geocode/json
    batch-size: 10
    delay-between-requests: 100
    debug: true
  mail:
    code:
      length: 4
    mail: email@example.com
    name: ICN navigator
    website: ICN.navigator.au
    timeout: 300
    ftl-path: email.ftl


server:
  port: 8082
  servlet:
    context-path: /api
